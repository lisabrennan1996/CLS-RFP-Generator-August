# Web server
fastapi>=0.139
uvicorn[standard]>=0.50
python-multipart>=0.0.32
pydantic>=2.13

# PDF/table extraction (this repo's own -- replaces CLS Studio's pdfium-render Rust crate and
# the PyInstaller-bundled camelot-worker sidecar; same underlying libraries, called in-process)
camelot-py>=2.0
pdfplumber>=0.11
pypdfium2>=5.11
pillow>=12.0

# Required by the RFP-population engine (plugin/engine/, referenced in place -- see
# backend/engine_paths.py -- not vendored into this repo)
python-docx>=1.2
edgeparse>=0.2.5

# Word (.docx/.doc) -> PDF conversion is done via a LibreOffice headless subprocess
# (backend/services/office_convert.py) -- LibreOffice itself is a SYSTEM dependency, not a pip
# package. Install it separately (see README's Setup section) if you need Word-document upload.
